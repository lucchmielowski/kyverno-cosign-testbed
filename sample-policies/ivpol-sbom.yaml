# Verify image that was signed with a GitHub SBOM attestation
apiVersion: policies.kyverno.io/v1
kind: ImageValidatingPolicy
metadata:
  name: verify-github-sbom-attestations
spec:
  validationActions: [Deny]
  webhookConfiguration:
    timeoutSeconds: 15
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        resources: ["pods"]
        operations: ["CREATE", "UPDATE"]
  matchImageReferences:
    - glob: "ghcr.io/lucchmielowski/cosign-testbed:*"
  
  # Define the attestation you want to verify
  attestations:
    - name: sbom
      intoto:
        type: https://spdx.dev/Document
  
  # Define the attestor (how to verify)
  attestors:
    - name: cosign
      cosign:
        keyless:
          identities:
            - subject: "https://github.com/lucchmielowski/cosign-testbed/.github/workflows/ci.yml@refs/heads/main"
              issuer: "https://token.actions.githubusercontent.com"
        ctlog:
          url: "https://rekor.sigstore.dev"
  
  # Validation expressions
  validations:
  - expression: >-
      images.containers.map(image, 
        verifyAttestationSignatures(image, attestations.sbom, [attestors.cosign])
      ).all(e, e > 0)
    message: "Failed to verify SPDX SBOM attestation"
